When the user asks about company Confluence pages, spaces, internal wiki content, installation, access setup, or wants to draft or update documentation, use the `xd-confluence` skill and prefer the connected Atlassian Rovo MCP server. In setup requests, switch to setup mode and guide the user one step at a time. In work requests, search before reading, read before writing, and fetch the latest page state immediately before any update.
